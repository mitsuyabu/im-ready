/**
 * 表示前の保険。プロンプト側で「マークダウンを使わない」と指示しているが、
 * それでも ** による太字記法が混ざった場合に、記号だけを外してプレーンテキストに戻す。
 * 「**text**」のペアだけを対象にし、中身のテキストは残す。閉じ相手のいない単体の ** は触らない。
 */
export function stripMarkdownBold(text: string): string {
  return text.replace(/\*\*(.+?)\*\*/g, "$1");
}
