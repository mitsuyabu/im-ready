# 留学カウンセリングAIチャットボット

留学を検討している日本語ユーザー向けの、AIカウンセリング・チャットボット。
詳細仕様は `docs/SPEC.md` を参照すること。このファイルには、常に守ってほしい前提だけを書く。

## 技術スタック
- Next.js（App Router）+ TypeScript
- Tailwind CSS
- Anthropic API（`@anthropic-ai/sdk`）。モデルは環境変数 `ANTHROPIC_MODEL` で切替、既定は最新の Claude。
- APIキーはサーバー側のみで使用し、クライアントに露出させない。

## 絶対に守るルール
- **中立を保つ**: 留学を勧めることをゴールにしない。「今は行かない」という選択も一緒に検討する。押し売り・誘導をしない。
- **お金を預からない**: 学費等の決済・預かりは実装しない。会話上もその前提で話す。
- **断定しない**: 学費・ビザ・為替など変動する情報は「一般的な傾向」として扱い、正確な最新情報は各機関での確認が必要と添える。
- **個人情報**: 機微な情報を扱う。UIに簡単な注意書きを置き、ログに個人情報を不用意に残さない。

## 作業の進め方
- `docs/SPEC.md` の「§8 実装の進め方」の順に、**1ステップずつ**実装する。指示された範囲外は先に作らない。
- 各ステップ後に必ず動く状態を保ち、`npm run dev` で動作確認できるようにする。
- 将来機能（マッチング・音声・管理画面・連絡自動化）は、インターフェースのスタブと TODO コメントだけ残し、中身は実装しない。
