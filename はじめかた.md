# はじめかた（Claude Code での進め方）

このフォルダは、留学カウンセリングボットを Claude Code で作るための雛形です。
下の順に進めれば、1ステップずつ動作確認しながら実装できます。

---

## 最初にやること

1. このフォルダ（`ryugaku-bot`）ごと、自分の作業場所に置く。
   - 直下に `CLAUDE.md`、`docs/SPEC.md`、`.env.example`、この `はじめかた.md` がある状態。
2. Anthropic の API キーを用意する（Anthropic Console で発行）。
3. このフォルダの中で Claude Code を起動する（フォルダ内で `claude` を実行）。
4. 下の「ステップ0」から順に、プロンプトをそのまま貼っていく。

> ポイント: `CLAUDE.md` はセッション開始時に毎回自動で読み込まれます。全文を貼り直す必要はありません。
> 各ステップの後は `npm run dev` で動きを確認してから次へ。無関係な作業に移るときは `/clear` で会話をリセット（CLAUDE.md は自動で再読込されます）。

---

## 貼り付けプロンプト（この順に）

### ステップ0：確認（まだコードは書かせない）
```
CLAUDE.md と docs/SPEC.md を読んで、これから何を作るのか要点だけ教えて。
まだコードは書かないで。
```

### ステップ1：雛形
```
docs/SPEC.md の §8 ステップ1だけ実装して:
Next.js（App Router）+ TypeScript + Tailwind の雛形と .env.example。
それ以外はまだ作らないで。
```
→ 終わったら `.env.example` を `.env.local` にコピーし、`ANTHROPIC_API_KEY` を設定。`npm run dev` で起動確認。

### ステップ2：会話API
```
§8 ステップ2だけ: /api/chat で Anthropic のストリーミング応答を実装。
まずカルテ抽出なしで、§5 のシステムプロンプトだけで会話が成立する状態にして。
```

### ステップ3：チャットUI
```
§8 ステップ3だけ: /widget のチャット画面。
ストリーミング表示、送信中の状態表示、エラー処理を入れて。
```

### ステップ4：カルテ自動更新
```
§8 ステップ4だけ: update_karte ツールを追加して、対話中にカルテ（§4のスキーマ）が
差分更新されるように。開発中はカルテをデバッグパネルで見えるようにして。
```

### ステップ5：カルテ要約の確認
```
§8 ステップ5だけ: 会話の区切りでカルテ要約を提示し、
ユーザーに認識のズレがないか確認してもらうステップを追加して。
```

### ステップ6：HP埋め込み
```
§8 ステップ6だけ: embed.js と iframe 起動を実装。
別の簡易 HTML から script タグ1行で設置テストできるようにして。
```

### ステップ7：README
```
§8 ステップ7: README に、セットアップ手順と HP への埋め込み手順を書いて。
```

---

## うまくいかないとき
- 指示外のものまで作ってしまう → 「そのステップだけ。それ以外は作らないで」ともう一度伝える。
- 途中で挙動がぶれてきた → `/clear` で会話をリセットしてから、続きのステップを指示。
- APIキー関連のエラー → `.env.local` に `ANTHROPIC_API_KEY` が入っているか確認。
